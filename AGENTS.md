# 代码纪要

1. 如果需要做代码测试，选用 adb 上的设备进行测试，不要在 host 端进行编译测试，如果没有显示指定设备，停下来进行询问
2. 测试时需要保持屏幕亮屏，测试中间屏幕不能关闭，如果设备是离电情况下，运行时检测一下电量是否合理
3. CPU 编译注意一下 repack 是否打开，若没打开则可能导致性能下降
4. 不要让 OpenCL 和 CPU 共用一个 weight 的 buffer，这两个后端由于消费的权重布局不一样，所以直接用会有语义问题