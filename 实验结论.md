# 实验结论

## 2026-04-17 Qwen2-3B `qnn-npu -> GPUOpenCL` 动态切换验证

测试目标是验证 `prefill=qnn-npu`、`decode=GPUOpenCL` 的阶段级动态切换是否语义正常，并记录首次切换的 runtime overhead。测试设备为 `db6c02cf`，模型为设备侧 `/data/local/tmp/powerserve/Qwen2-3B`，QNN 侧使用 AoT 图与 `-dev qnn-npu` 路径。

| 设备 | 模型 | Prefill 路由 | Decode 路由 | 主设备 | Prompt token 数 | `-n` | Prompt ms | Eval ms | Total ms | Max `kv_migration` ms | 开销占 Total | 开销占 Eval | 语义结果 | 关键切换证据 |
| --- | --- | --- | --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- | --- |
| `db6c02cf` | `Qwen2-3B` AoT | `qnn-npu` | `GPUOpenCL` | `qnn-npu` | 14 | 24 | 249.41 | 1336.67 | 1648.16 | 247.63 | 15.02% | 18.53% | 输出在解释天空为何呈蓝色，语义正常，但第二句被截断 | `apply_hetero_plan ... backend=opencl`; `queued opencl prefix replay for 14 token(s)` |
| `db6c02cf` | `Qwen2-3B` AoT | `qnn-npu` | `GPUOpenCL` | `qnn-npu` | 14 | 40 | 242.10 | 2122.52 | 2434.49 | 281.65 | 11.57% | 13.27% | 输出继续解释蓝天散射原因，语义正常，存在少量词形错误如 `scattereded` | `apply_hetero_plan ... backend=opencl`; `replay_dynamic_qnn_prefix: replaying 14 seq0 prefix token(s)` |

注：这里的 `Max kv_migration` 对应首次 decode 切换时的 prefix replay / 目标后端 KV 重建开销，不代表后续 steady-state 每个 decode token 都会持续付出同等成本。

## 2026-04-18 Qwen2-3B `qnn-npu -> GPUOpenCL` 切换开销分解

测试目标是继续分解 `prefill=qnn-npu`、`decode=GPUOpenCL` 的 decode 首次切换与稳态第二次切换开销，确认在消除 replay 后，剩余 overhead 主要落在 `kv_migration` 还是 `sched_reserve`。测试设备为 `db6c02cf`，模型为设备侧 `/data/local/tmp/powerserve/Qwen2-3B`，QNN 侧使用 AoT 图与 `-dev qnn-npu`，命令形态为 `llama-bench -v -r 2 -pg 500,1 -ngl 99 -dev qnn-npu -t 1 --no-warmup --mmap 0`。

| 变体 | 轮次 | `total_wall` ms | `reserve` ms | `plan_reserve` ms | `kv_migration` ms | `alias` ms | `transfer` ms | `reserve/total` | `plan_reserve/reserve` | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| baseline | 第 1 次 decode 切换 | 54.780 | 16.125 | 14.952 | 38.604 | 8.754 | 29.815 | 29.44% | 92.73% | 首次切换仍以 KV 搬移为主，KV 内部主要是 `transfer` |
| baseline | 第 2 次 decode 切换 | 18.690 | 14.714 | 13.575 | 3.935 | 0.000 | 3.900 | 78.73% | 92.26% | 稳态切换后主导项变成 `reserve`，而且几乎都在 `plan_reserve` |
| direct-host-ptr | 第 1 次 decode 切换 | 55.416 | 17.785 | 16.639 | 37.583 | 37.545 | 0.000 | 32.09% | 93.56% | `transfer` 被消除，但首次 alias 建立成本上升，首次总时延基本不变 |
| direct-host-ptr | 第 2 次 decode 切换 | 15.009 | 14.949 | 13.679 | 0.014 | 0.000 | 0.000 | 99.60% | 91.50% | 稳态切换几乎完全由 `reserve -> plan_reserve` 主导，KV 开销已基本清零 |

补充说明：

- 这里的 `plan_reserve` 对应 `sched_reserve()` 内部的 `reserve_plan_buffers(...)`，即当前活跃计划下的三次 `graph_reserve(pp -> tg -> pp)` 路径。
- `direct-host-ptr` 对应实验环境变量 `GGML_OPENCL_EXPERIMENTAL_QNN_DIRECT_HOST_PTR=1`，含义是让 OpenCL 用 `CL_MEM_USE_HOST_PTR` 直接可见 QNN `rpcmem` Host Buffer，而不是额外做一次 host 到 device 的 `WriteBuffer`。
- 从第二次切换结果看，`qnn-npu -> GPUOpenCL` 的稳态残余开销已经不再是 KV 同步，而是切换后 OpenCL 侧重新 `reserve` 当前执行计划的调度与缓冲区过程。

## 2026-04-18 Qwen2-3B `qnn-npu -> GPUOpenCL` 首次切换 alias 预热

测试目标是继续降低 `prefill=qnn-npu`、`decode=GPUOpenCL` 的首次 decode 切换开销。当前实验没有修改首次切换后的实际数据路径，而是将 OpenCL 对 `qnn-npu-host` KV 的第一次 `CL_MEM_USE_HOST_PTR` alias 创建前移到上下文初始化阶段；默认逻辑不变，仅在 `GGML_OPENCL_EXPERIMENTAL_QNN_DIRECT_HOST_PTR=1` 下生效。测试设备为 `db6c02cf`，模型为设备侧 `/data/local/tmp/powerserve/Qwen2-3B`，QNN 侧使用 AoT 图与 `-dev qnn-npu`，联合工作负载命令形态为 `llama-bench -v -r 1 -pg 1000,1 -ngl 99 -dev qnn-npu -t 1 --no-warmup --mmap 0`。

| 变体 | `pp1000+tg1` 吞吐 `t/s` | 构造期 alias 预热 ms | 首次 decode `total_wall` ms | 首次 decode `reserve` ms | 首次 decode `kv_migration` ms | 首次 decode `alias` ms | 说明 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| `direct-host-ptr + tg-only-reserve` | 285.95 | 0.00 | 29.020 | 2.276 | 26.660 | 26.563 | 未预热时，首次切换主要被首次 alias 创建主导 |
| `direct-host-ptr + tg-only-reserve + ctor alias prewarm` | 293.43 | 35.152 | 2.014 | 1.908 | 0.033 | 0.000 | 首次切换中的 alias 成本被前移到上下文初始化，切换本身只剩 reserve 开销 |

补充说明：

- 构造期日志显示 `maybe_prewarm_dynamic_qnn_opencl_kv_aliases: prewarmed experimental qnn->opencl KV alias path alias_us=35152 backend_sync_us=1 transfer_us=0`，说明前移的是一次性的 OpenCL alias 建立，而不是额外的数据 copy。
- 首次 decode 切换日志显示 `sync_external_opencl_host_aliases: timing alias_us=0 backend_sync_us=0 transfer_us=0`，以及 `synchronize: timing phase=decode n_tokens=1 total_wall_us=2014 ... kv_migration_us=33`，说明 `qnn-npu` 产出的共享 KV 仍然被 OpenCL 直接消费，没有退回 replay。
- 相比未预热版本，首次切换端到端开销从 `29.020 ms` 降到 `2.014 ms`，下降约 `93.1%`；`pp1000+tg1` 吞吐从 `285.95 t/s` 提升到 `293.43 t/s`，提升约 `2.6%`。
- 这不是“消除” alias 成本，而是把它从首轮 decode 切换前移到了上下文初始化。若上下文会被重复复用，这种前移对 decode-stage runtime overhead 是有利的；若每次请求都重建上下文，则需把这笔一次性初始化成本单独计入端到端分析。
