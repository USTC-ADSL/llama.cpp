我有下面几个任务，帮我完成，由于一次请求的上下文有限,你需要先把所有需要的任务放到一个 tasklist 中，然后每次完成一个子任务，将 tasklist 中对应的子任务打勾，写一个 handoff 文档来指导下一次任务应该做什么，本次任务完成了什么，并且对代码进行封板，我上述提到的文档都放在/home/miog/pzw/download/pzw/HeteroCompute/llama.cpp-acom/Paper_Writing/Task/handoff-622下面，注意测试在上测试，不要在设备上到处放没用的二进制文件，过时的 log 文件，每次测试完成后如没有必要，可以直接删除，不要每次都写一个 RED 测试，简单任务可以直接端到端测试，注意下面说的图大小都是 kv cache slot，也就是 2k 图和 4k 图区别，不是 batch size=128 还是 1 的区别,测试使用 adb db6c02cf设备。下面是具体任务内容：
我已经实现了单个 req 内的 2k图和 4k 图的切换设置（当前会根据 kv cache slot 大小和 batch 大小来选图）以及请求间的 req的切换，你可以查看/home/miog/pzw/download/pzw/HeteroCompute/llama.cpp-acom/Paper_Writing/Task/handoff下的完成记录，但是当前对于 3B 模型来说，只加载了 QNN graph 的一个 bin 文件，没有加载全部，这会导致 qnn 计算只计算一部分，并且语义出问题，你的任务是验证是否存在这个问题，如果真的存在这个问题，你需要进行修复

