# Progress Log

本目录用于记录按当前优先级推进的任务完成情况。

记录规则：

- 每完成一个明确任务，新增一个独立 Markdown 记录文件。
- 文件名格式：`YYYY-MM-DD-<seq>-<short-title>.md`
- 每条记录至少包含：
  - 任务名称
  - 背景与目标
  - 执行内容
  - 关键证据
  - 当前结论
  - 下一步

当前任务顺序：

1. 调查 `Qwen3` 适配是否导致 `Qwen2 attn_core` unmatched
2. 更新主线工作计划，使其与新版 `AGENTS.md` 的 `Prefill/Decode` 主线一致
3. 收口最高优先级稳定性问题
4. 继续推进基线、阶段矩阵、Prefill/Decode phase switch overhead 与路由稳定性任务
